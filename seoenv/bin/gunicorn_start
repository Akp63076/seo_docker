[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=root
Group=www-data
WorkingDirectory=/home/seo
#ExecStart=/home/rank-tool-dashboard/rnkenv/bin/gunicorn --access-logfile - --workers 3 --bind unix:/home/rank-tool-dashboard/ranktracker.sock --timeout=300 ranktracker.wsgi:application
ExecStart=/home/seo/seoenv/bin/gunicorn --access-logfile - --workers 3 --bind unix:/home/seo/seoTool.sock --timeout=300 seoTool.wsgi:application

[Install]
WantedBy=multi-user.target   
